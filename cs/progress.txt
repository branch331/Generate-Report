8/18/18:

-removed text display for 'status' and instead just disable the controls on the UI while making a report
-replaced Overwrite button w/ checkbox

next steps:
-test on other computer since sys config driver is broken (labview example also doesnt make report)

